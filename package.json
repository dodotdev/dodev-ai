{
  "name": "dodev",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native task and memory management via the Model Context Protocol",
  "repository": {
    "type": "git",
    "url": "https://github.com/dodotdev/dodev-ai.git"
  },
  "homepage": "https://dodev.ai",
  "license": "MIT",
  "scripts": {
    "dev:all": "lsof -ti:3041 | xargs kill -9 2>/dev/null || true; lsof -ti:3100 | xargs kill -9 2>/dev/null || true; pkill -f 'convex dev' 2>/dev/null || true; turbo run dev dev:cloud --ui=tui --concurrency=20 --filter=@dodev/web --filter=@dodev/convex --filter=@dodev/mcp-server",
    "dev:web": "lsof -ti:3041 | xargs kill -9 2>/dev/null || true; turbo run dev --ui=tui --concurrency=20 --filter=@dodev/web",
    "dev:convex": "pkill -f 'convex dev' 2>/dev/null || true; pnpm --filter @dodev/convex dev",
    "dev:mcp": "pnpm --filter @dodev/mcp-server dev:stdio",
    "dev:cloud": "pnpm --filter @dodev/mcp-server dev:cloud",
    "build": "turbo build",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "generate-key": "pnpm --filter @dodev/mcp-server generate-key"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.5",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20.0.0"
  }
}
